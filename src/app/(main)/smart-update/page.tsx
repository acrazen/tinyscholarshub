import { UpdateGenerator } from "@/components/smart-update/update-generator";

export default function SmartUpdatePage() {
  return (
    <div>
      {/* Removed the h1 from here as it's in the component now */}
      <UpdateGenerator />
    </div>
  );
}
